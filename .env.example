# Names only. Keep this tracked example empty. Never paste a real token into chat.
# Future live Discord work will read the bot token from an OS credential adapter,
# with this environment variable allowed only as an explicit development fallback.
CODEX_BRIDGE_DISCORD_BOT_TOKEN=
